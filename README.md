# Agentic Trading Desk

A multi-agent swing-trading assistant for US equities with a human in the loop. A deterministic scanner screens the S&P 500 for candidate setups; three specialist LLM agents (technical, news, events) analyze each candidate in parallel; a trader agent synthesizes their outlooks and proposes a trade — or explicitly declines to. Every proposal passes a code-level risk gate the LLM cannot override, then pauses for human approval before anything executes.

**Status: in active development.** Paper/simulated trading only — no real money is ever at risk.

## How it works

```
S&P 500 universe — generated by src/refresh_universe.py
        │
        ▼
  Scanner (deterministic code) — liquidity filter + technical signals
        │  top-K candidates, with reasons
        ▼
  ┌─ Technical Agent ─┐
  ├─ News Agent ──────┤   (parallel LLM analysis)
  └─ Events Agent ────┘
        │
        ▼
  Trader Agent — synthesizes specialist outlooks
        │  proposal (entry / stop / target / size / rationale)  or NO-TRADE
        ▼
  Risk Gate (pure code: position size, R:R floor, stop placement, trade limits)
        │
        ▼
  Human approval (LangGraph interrupt — full reasoning displayed)
        │  approve
        ▼
  Execute → trade journal (paper)
```

## Design principles

- **Hard risk limits live in code, not prompts.** The LLM cannot override the gate; the human approves within it.
- **NO-TRADE is a first-class decision.** Most days, standing aside is correct — the agent says so, with reasoning.
- **Cheap filters run wide, expensive analysis runs narrow.** Deterministic scanner over 500 names; LLM pipeline over the top handful.
- **Process over P&L.** A few days of returns is statistical noise; the eval suite measures groundedness, policy compliance, and safety invariants instead.

## Build status

- [x] Universe refresh script (`src/refresh_universe.py`) — pulls S&P 500 constituents from Wikipedia; output CSV is generated locally, not committed
- [x] Technical indicators — RSI (Wilder), SMA/crossovers, volume spike, 52-week high/low (pure pandas)
- [ ] Scanner — two-stage screen over daily bars (in progress)
- [ ] Specialist agents — technical / news / events (LangGraph, parallel)
- [ ] Trader agent — outlook synthesis + structured proposals
- [ ] Risk gate + human-in-the-loop approval
- [ ] Execution layer (MCP server → trade journal; Alpaca paper API later)
- [ ] Eval suite — safety invariants, LLM-as-judge process scoring, frozen-snapshot regression set
- [ ] LangSmith tracing + session persistence
- [ ] Dockerize (post-core-build)

## Tech stack

Python · LangGraph / LangChain · OpenAI · Alpaca Market Data API (daily bars, news) · LangSmith (tracing & evals) · MCP (execution tools)

## Setup

```bash
git clone <repo-url>
cd Agentic-Trading
python -m venv venv && source venv/bin/activate   # Windows: venv\Scripts\activate
pip install -r requirements.txt
cp .env.example .env    # add your keys: ALPACA_API_KEY, ALPACA_SECRET_KEY
```

Generate the trading universe — **required once before the first scan** (the scanner reads the CSV this produces; re-run occasionally as index membership changes):

```bash
python src/refresh_universe.py
```

Run the scanner:

```bash
python src/scanner.py
```

Scan results are written to `data/scans/` with the signals that fired for each candidate.

## Project structure

```
Agentic-Trading/
├── config/          # settings.yaml, events.json — curated inputs (+ generated sp500.csv, gitignored)
├── data/            # scan outputs, journal — generated, gitignored
├── docs/            # architecture notes
├── src/
│   ├── agents/      # technical_agent.py, news_agent.py, events_agent.py
│   ├── scanner.py
│   └── refresh_universe.py
├── .env.example
└── README.md
```
